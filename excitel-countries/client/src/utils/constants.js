const COLUMNS = [
    { key: 'name', label: 'Name' },
    { key: 'capitalName', label: 'Capital' },
    { key: 'region', label: 'Region' },
    { key: 'subregion', label: 'Subregion' },
];

const PAGE_SIZE_OPTIONS = [10, 20, 40, 60, 100];

const LONG_PRESS_MS = 1500;

export { COLUMNS, PAGE_SIZE_OPTIONS, LONG_PRESS_MS };
